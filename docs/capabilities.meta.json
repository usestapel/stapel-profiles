{
  "_comment": "Hand-curated layer of docs/capabilities.json (capability-config.md §2 p.3). The emitter (stapel_profiles/_capabilities.py) merges this over the derived axes; an axis present in conf.py DEFAULTS but missing here (or vice versa) is a loud emission error. Keep business_label/summary in business language — they surface in CTO conversations, not in code.",
  "provides": "User profiles and social graph: display name, avatar, locale/currency/measurement preferences, notification opt-outs, and follow/unfollow/block relationships between users, with a relationship-status lookup.",
  "requires": [
    {
      "module": "stapel-core",
      "optional": false,
      "reason": "comm bus (events/functions), JWT authentication, GDPR provider registry, AppSettings config layer"
    },
    {
      "module": "stapel-auth",
      "optional": true,
      "reason": "consumes the user.registered / user.deleted comm events (OAuth avatar import, GDPR erasure on account deletion) that stapel-auth emits; decoupled via the comm bus, not a hard import dependency"
    },
    {
      "module": "stapel-cdn",
      "optional": true,
      "reason": "avatar storage/existence checks (PROFILES_AVATAR_CHECK=comm calls cdn.media_exists) and OAuth avatar import (cdn.import_from_url) go through stapel_core.comm to a CDN provider"
    },
    {
      "module": "stapel-gdpr",
      "optional": true,
      "reason": "surfaces this module's registered GDPR provider (profile section export/erase) through DSR endpoints; the registration itself only needs stapel-core.gdpr"
    }
  ],
  "extension_points": [
    {
      "name": "serializer_seams",
      "kind": "class_override",
      "summary": "Every profile/relationship view declares request/response serializer seams (SerializerSeamsMixin) — subclass the view, override the attribute, remount the URL; keep calling super() in serializer overrides so profile.changed keeps publishing."
    },
    {
      "name": "profile_extras_relation",
      "kind": "app_layer_model",
      "summary": "Extend the profile without a fork: an app-layer model with a OneToOne to profiles.Profile (or keyed by the same user_id UUID), exposed through the serializer seams — models here are deliberately not swappable."
    },
    {
      "name": "profile.changed",
      "kind": "comm_event",
      "summary": "The primary hook for syncing derived data: emitted on every profile mutation (keyed by user_id) — subscribe with @on_action from any module or the host app, no import of this package."
    }
  ],
  "axes": {
    "PROFILES_AVATAR_CHECK": {
      "business_label": "Avatar existence verification method",
      "summary": "Controls how the profile service confirms an uploaded avatar reference actually exists on the CDN before saving it: through the internal comm bus (default) or skipped entirely (only the reference format is still validated)."
    }
  }
}
