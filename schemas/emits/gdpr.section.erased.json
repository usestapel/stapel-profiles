{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdpr.section.erased",
  "description": "This module's erasure receipt: what it removed for the named subject, echoing the correlation_id it was asked with. Emitted in the same transaction as the deletions (outbox discipline), so a rolled-back erasure never produces a receipt.",
  "type": "object",
  "required": ["correlation_id", "owner", "subject_type", "subject_key", "counts"],
  "properties": {
    "correlation_id": { "type": "string" },
    "owner":          { "type": "string", "const": "profile" },
    "subject_type":   { "type": "string", "enum": ["account"] },
    "subject_key":    { "type": "string" },
    "counts":         {
      "type": "object",
      "description": "Rows removed, by kind: profiles, relationships_outgoing, relationships_incoming.",
      "additionalProperties": { "type": "integer" }
    }
  },
  "additionalProperties": false
}
